<h1>/Prop</h1>

<p>
  <b>Title</b><br>
  <%= @prop.title %>
</p>

<p>
  <b>Description</b><br>
  <%= @prop.description %>
</p>

<div class="form-actions">
  <%= link_to 'Back', props_path, :class => 'btn'  %>
  <%= link_to 'Edit', edit_prop_path(@prop), :class => 'btn' %>
  <%= link_to 'Delete', prop_path(@prop), :method => 'delete', :confirm => 'Are you sure?', :class => 'btn btn-danger' %>
</div>
